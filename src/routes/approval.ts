// DEPRECATED — approval logic moved to /api/public (token-based).
// This file is kept empty intentionally and no longer registered in server.ts.
import { Router } from 'express';
const router = Router();
export default router;
